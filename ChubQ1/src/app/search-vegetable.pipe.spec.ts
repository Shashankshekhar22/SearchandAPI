import { SearchVegetablePipe } from './search-vegetable.pipe';

describe('SearchVegetablePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchVegetablePipe();
    expect(pipe).toBeTruthy();
  });
});
